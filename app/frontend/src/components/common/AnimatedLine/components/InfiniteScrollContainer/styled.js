import styled from 'styled-components';

import { Box } from '@mui/material';

const SInfiniteScrollContainer = styled(Box)`
  display: flex;
  width: 100%;
  overflow: hidden;
  white-space: nowrap;
`;

export default SInfiniteScrollContainer;
